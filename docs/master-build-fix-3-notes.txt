MASTER BUILD FIX 3

Changed:
- app/master/page.tsx

Fixes included:
1. currentSessionCode -> sessionCode
2. useRef<Prayer | undefined>(undefined) -> useRef<Prayer | null>(null)
3. bestEnd !== session.currentIndex -> bestEnd !== currentSession.currentIndex

Reason:
- session may be null at that line, but currentSession is the guarded safe variable in scope
