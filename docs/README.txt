This zip contains a full protected app/master/page.tsx.

What it adds:
- redirects unauthenticated users to /auth
- shows account bar in master page
- profile link
- logout button

Requirements:
- keep your existing lib/auth.ts from the earlier auth zip
- keep your existing app/profile/page.tsx if you already added profile support
